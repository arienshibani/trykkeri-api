# Optional: copy to .env and set values to override Grafana defaults (admin/admin).
# If .env is missing or these are unset, docker-compose uses admin/admin.
# GRAFANA_ADMIN_USER=admin
# GRAFANA_ADMIN_PASSWORD=your-secure-password
